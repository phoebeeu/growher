# DESIGN.md｜开发版0814

版本：3.0
日期：2026-08-14
状态：当前开发唯一规范
适用范围：React Web Application；移动端任务流程 + 响应式 Website 首页；可复用产品设计基线

> 备注：本文件由原 DESIGN.md 2.0 与《今日推荐首页设计说明》合并而成。当前产品开发、代码评审和视觉验收均以本文件为准。DESIGN2.md 与 2026-08-13-DESIGN-暖窗拼贴水彩-孕期版.md 不属于当前开发规范。

## 0. 使用方法与优先级

### 0.1 用于当前产品

- 直接执行本文件全部规则。
- 第 1–15 节是全局视觉、组件和五页流程规范。
- 第 16 节是响应式 Website 首页规范；与前文发生冲突时，仅在 Website 首页范围内由第 16 节覆盖。
- 产品功能范围以仓库根目录 MVP-PRD.md 为准；视觉规则以本文件为准。

### 0.2 用于其他产品

- 保留色彩令牌结构、字体层级、4px 间距系统、组件状态、动效时长、无障碍和硬约束。
- 替换产品名称、目标人群、页面文案、业务类别、业务图案和页面映射。
- 先声明载体是“移动端任务流程”“响应式 Website”或两者并存，再使用对应布局规则。
- 不复制 The Weather Channel 的名称、标志、节目标题、图标文件或原始页面内容。
- 新产品若修改具体色值、字体或组件尺寸，必须在文件顶部增加“产品覆盖规则”，写出旧值、新值和适用范围；不得静默修改。

### 0.3 规则优先级

1. 文件顶部明确写出的产品覆盖规则。
2. 第 16 节 Website 首页规则，仅适用于 Website 首页。
3. 第 1–15 节全局规则。
4. 历史设计文件只用于追溯，不参与开发判断。

## 1. 取样依据

| 来源 | 取样项 | 取样值 |
|---|---|---|
| `https://weather.com/retro/` | 页面比例 | `4 / 3` |
| `https://weather.com/retro/` | 字体 | `Martian Mono`，字重 `100–800`，字宽 `75%–112.5%` |
| `https://weather.com/retro/` | `--color-twc-blue` | `#354E84` |
| `https://weather.com/retro/` | `--color-twc-purple` | `#332370` |
| `https://weather.com/retro/` | `--color-twc-orange` | `#E28144` |
| `https://weather.com/retro/` | `--color-twc-yellow` | `#FFFF6F` |
| `https://weather.com/retro/` | 基础间距 | `4px` |
| `https://weather.com/retro/` | 内容安全边距 | 容器宽度的 `7.8125%` |
| `https://weather.com/retro/` | 开机动画 | `600ms ease` |
| `https://weather.com/retro/` | 淡入淡出 | `500ms` |
| `https://weather.com/retro/` | 信息条进入 / 退出 | `300ms ease-out / 50ms ease-out` |
| 截图 1 | 画布 | `1404×599px` |
| 截图 1 | 背景 | `#F8F8F8` |
| 截图 1 | 样片卡 | `298×224px`，水平间距 `18–19px`，垂直间距 `18px`，圆角 `8px` |
| 截图 2 | 画布 | `1600×1200px` |
| 截图 2 | 外层背景 | `#285D91` |
| 截图 2 | 内容画面 | `1194×896px`，比例误差小于 `0.1%` |

- 不使用 The Weather Channel 名称、标志、节目标题或图标文件。
- 保留比例、信息层级、颜色关系、字体关系和动效时间；产品文案与图案全部替换为孕期目标内容。

## 2. 产品载体

- 设计基准视口为 `390×844px`。
- 支持宽度为 `320–480px`。
- 视口宽度大于 `480px` 时，App 外壳固定为 `480px` 并水平居中。
- App 外壳高度使用 `100dvh`。
- App 外部背景为 `#285D91`。
- App 页面背景为 `#F7F2E9`。
- App 外壳边框为 `1px solid rgba(255,255,255,0.72)`。
- App 外壳圆角：手机视口为 `0`；宽度大于 `700px` 时为 `20px`。
- App 外壳阴影为 `0 20px 64px rgba(18,26,54,0.24)`。
- 使用 `env(safe-area-inset-top)` 和 `env(safe-area-inset-bottom)`。
- 页面内容使用单列；不生成桌面多栏页面。

## 3. 转译规则

| RetroCast 结构 | 孕期助手结构 |
|---|---|
| `4:3` 天气画面 | `BroadcastPanel`，用于全景图、状态结果、总结结果 |
| 顶部节目标题 | 页面编号、页面标题、当前日期 |
| 天气数据左右列 | 左侧图案、右侧任务或状态数据 |
| 黄色城市名称 | 当前目标、固定产检或当前行动 |
| 底部字幕条 | 系统结论、下一步行动、重排原因 |
| 三日天气卡 | 三档能量、三项本周重点、三项总结数据 |
| 地图节点 | 纵向目标 Timeline 节点 |
| 月相图 | 孕周、产检节点、阶段节点 |
| 雷达图 | 本周时间容量图，不表示医疗监测 |
| CRT 扫描线 | 静态 `4px` 周期叠层，透明度 `0.025` |

- `4:3` 只用于结果面板，不用于表单、长列表或整页容器。
- 表单、任务卡和 Timeline 放在 `4:3` 面板之外。
- 单页最多使用 `1` 个 `BroadcastPanel`。

## 4. 色彩

### 4.1 基础令牌

| 令牌 | 色值 | 使用位置 |
|---|---:|---|
| `color.stage` | `#285D91` | App 外部背景 |
| `color.canvas` | `#F7F2E9` | 页面背景 |
| `color.surface` | `#FFFDF8` | 表单、任务卡、弹层 |
| `color.surface-muted` | `#ECE8DE` | 次级区域、只读区域 |
| `color.screen-blue` | `#354E84` | `BroadcastPanel` 主内容区 |
| `color.screen-purple` | `#332370` | 标题栏、固定节点、次按钮 |
| `color.screen-orange` | `#E28144` | 主按钮、标题渐变、当前节点 |
| `color.screen-yellow` | `#FFFF6F` | 数据标题、固定日期、当前行动 |
| `color.ink` | `#20243B` | 页面标题、正文 |
| `color.ink-secondary` | `#666B7A` | 辅助文字 |
| `color.text-on-screen` | `#FFFFFF` | 蓝色与紫色区域文字 |
| `color.border` | `#BCC7D7` | 卡片与输入框边框 |
| `color.focus` | `#332370` | 键盘焦点 |
| `color.error` | `#A63D4B` | 系统错误 |
| `color.error-soft` | `#F8E0E3` | 错误提示背景 |

### 4.2 孕产令牌

| 令牌 | 色值 | 使用位置 |
|---|---:|---|
| `color.maternal` | `#C05F78` | 孕产准备类别、孕肚图案 |
| `color.maternal-soft` | `#F5DEE5` | 孕产卡背景 |
| `color.exercise` | `#3E866F` | 运动与体重类别 |
| `color.exercise-soft` | `#DDEEE7` | 运动卡背景 |
| `color.nutrition` | `#C7833A` | 饮食、饮水、营养图案 |
| `color.nutrition-soft` | `#F5E6CF` | 营养卡背景 |
| `color.rest` | `#6476A8` | 休息、睡眠图案 |
| `color.rest-soft` | `#E1E6F3` | 休息卡背景 |

### 4.3 四类目标

| 类别 | 前景 | 背景 | 图标 |
|---|---:|---:|---|
| 工作 | `#65728A` | `#E8ECF2` | `briefcase` |
| 副业、项目与兴趣 | `#C86A32` | `#F8E4D6` | `sparkles-grid` |
| 运动与体重 | `#3E866F` | `#DDEEE7` | `stretch` |
| 孕产准备与产检 | `#C05F78` | `#F5DEE5` | `belly-calendar` |

- 类别必须同时显示文字、图标和颜色。
- 类别色占单张卡片面积不得超过 `24%`。
- 固定产检标签使用 `#FFFF6F` 背景、`#332370` 文字和 `1px solid #332370` 边框。
- 主按钮使用 `#E28144` 背景与 `#20243B` 文字。
- 次按钮使用 `#332370` 背景与 `#FFFFFF` 文字。
- `#A63D4B` 只用于网络、保存和系统错误。

### 4.4 渐变与叠层

- 标题栏渐变固定为 `linear-gradient(180deg, #332370 0%, #76507C 52%, #E28144 100%)`。
- 单页最多使用 `1` 个渐变区域。
- 扫描线固定为 `repeating-linear-gradient(to bottom, rgba(255,255,255,0.025) 0 1px, transparent 1px 4px)`。
- 噪点叠层透明度为 `0.03`，`mix-blend-mode: soft-light`。
- 扫描线和噪点均设置 `pointer-events: none`。
- 正文不得直接覆盖在照片或噪点不透明度高于 `0.04` 的区域上。

## 5. 字体

### 5.1 字体族

| 用途 | 字体栈 |
|---|---|
| 中文界面 | `Noto Sans SC, PingFang SC, Microsoft YaHei, system-ui, sans-serif` |
| 数字、日期、步骤编号、英文标签 | `Martian Mono, ui-monospace, SFMono-Regular, Consolas, monospace` |
| 图表数值 | `Martian Mono, ui-monospace, monospace` |

- `Martian Mono` 允许字重 `300、400、500、700`。
- `Martian Mono` 的英文标题使用 `font-stretch: 75%`。
- 数字使用 `font-variant-numeric: tabular-nums`。
- 中文不得使用缺少中文字形的 `Martian Mono` 作为唯一字体。

### 5.2 字号

| 层级 | 字号 / 行高 | 字重 | 字距 |
|---|---|---:|---:|
| 页面标题 | `24px / 32px` | `700` | `-0.02em` |
| App Bar 标题 | `17px / 24px` | `700` | `-0.01em` |
| Broadcast 标题 | `18px / 24px` | `700` | `-0.05em` |
| 区块标题 | `18px / 26px` | `700` | `-0.01em` |
| 卡片标题 | `16px / 24px` | `600` | `0` |
| 正文 | `15px / 24px` | `400` | `0` |
| 数据值 | `20px / 28px` | `500` | `-0.03em` |
| 辅助文字 | `13px / 20px` | `400` | `0` |
| 标签与时间 | `12px / 18px` | `500` | `0.02em` |
| 按钮 | `15px / 20px` | `700` | `0.01em` |

- 页面标题最多 `2` 行，每行最多 `16` 个汉字。
- Broadcast 标题最多 `2` 行。
- 正文单段最多 `4` 行。
- 数据标签使用英文大写时，字符数不得超过 `18`。

## 6. 间距、比例、圆角与边框

### 6.1 间距令牌

基础单位为 `4px`，只允许使用下列值：

| 令牌 | 数值 |
|---|---:|
| `space.1` | `4px` |
| `space.2` | `8px` |
| `space.3` | `12px` |
| `space.4` | `16px` |
| `space.5` | `20px` |
| `space.6` | `24px` |
| `space.8` | `32px` |
| `space.10` | `40px` |
| `space.12` | `48px` |
| `space.16` | `64px` |

### 6.2 页面尺寸

- App Bar 高度为 `56px + env(safe-area-inset-top)`。
- 页面水平内边距为 `16px`。
- 页面顶部内边距为 `20px`。
- 页面底部内边距不少于 `112px`。
- 页面区块间距为 `24px`。
- 同组卡片间距为 `12px`。
- 底部操作区最小高度为 `80px + env(safe-area-inset-bottom)`。
- 底部操作区内边距为 `12px 16px`。

### 6.3 BroadcastPanel

- 宽度为内容区宽度的 `100%`。
- `aspect-ratio` 固定为 `4 / 3`。
- 面板内部水平安全边距为面板宽度的 `7.8125%`。
- 标题栏高度为面板高度的 `18.75%`。
- 内容区高度为面板高度的 `64.5833%`。
- 字幕条高度为面板高度的 `16.6667%`。
- 面板圆角为 `12px`。
- 面板边框为 `1px solid rgba(255,255,255,0.78)`。
- 面板内容区背景为 `#354E84`。
- 面板内容区内阴影为 `inset 0 0 18px rgba(255,255,255,0.12)`。
- 视口宽度小于 `360px` 时，面板内部数据区改为单列。

### 6.4 圆角与阴影

| 元素 | 圆角 | 边框 | 阴影 |
|---|---:|---|---|
| 输入框 | `8px` | `1px solid #BCC7D7` | 无 |
| 主按钮、次按钮 | `10px` | `1px solid rgba(32,36,59,0.24)` | `3px 3px 0 #20243B` |
| 普通卡片 | `12px` | `1px solid #BCC7D7` | `0 4px 12px rgba(32,36,59,0.08)` |
| BroadcastPanel | `12px` | `1px solid rgba(255,255,255,0.78)` | `0 8px 24px rgba(32,36,59,0.18)` |
| 标签 | `6px` | `1px solid currentColor` | 无 |
| Bottom sheet | `16px 16px 0 0` | `1px solid #BCC7D7` | `0 -12px 36px rgba(32,36,59,0.18)` |
| 线稿图标 | `0` | 无 | `2px 2px 0 #20243B`，仅用于填充型图标 |

## 7. 图标与孕产图案

### 7.1 图标规则

- 通用图标使用 `24×24px` 画板。
- 描边宽度为 `2px`。
- `stroke-linecap` 和 `stroke-linejoin` 使用 `round`。
- 默认不填充；选中态填充透明度为 `20%`。
- 卡片图标容器为 `40×40px`。
- 空状态图案最大尺寸为 `96×96px`。
- 单个图标最多使用 `3` 个色值。
- 图标不得包含品牌标志或医疗机构标志。

### 7.2 图案目录

| 图案 ID | 构成 | 使用页面 |
|---|---|---|
| `belly-profile` | 孕肚侧面轮廓 + `1` 个圆点 | 目标设定、孕产类别 |
| `fetal-heartline` | 心形 + `3` 段折线 | 状态确认、总结 |
| `baby-footprints` | 左右脚印各 `5` 个趾点 | 总结、空状态 |
| `checkup-calendar` | 日历 + 圆点 + 固定锁 | 产检日期 |
| `hospital-bag` | 手提包 + `3` 个内部格 | 待产准备 |
| `nutrition-bowl` | 碗 + 叶片 + `2` 条蒸汽线 | 饮食任务 |
| `water-drop` | 水滴 + `3` 格刻度 | 饮水任务 |
| `stretch-figure` | 人体折线 + 垫子线 | 运动任务 |
| `pillow-moon` | 枕头矩形 + 月牙 | 休息与睡眠 |
| `weight-path` | `3` 个节点 + 上下波形线 | 体重趋势 |
| `week-ring` | `7` 个圆点组成闭环 | 本周安排 |
| `task-signal` | `3` 格信号条 + 箭头 | 任务难度与重排 |

### 7.3 背景图案

- 背景图案画板为 `48×48px`。
- 背景图案描边为 `1px`。
- 图案间距为 `16px`。
- 背景图案透明度为 `0.06–0.08`。
- 单个区块最多使用 `2` 种图案 ID。
- 图案覆盖面积不得超过区块面积的 `40%`。
- 表单输入区、正文区和错误信息区不得铺设背景图案。
- 胎心线只作为图案，不显示 BPM 数值，不表示医疗监测结果。

## 8. 通用组件

### 8.1 App Bar

- 左侧按钮点击区域为 `44×44px`。
- 中间显示页面标题，使用 `17px / 24px`。
- 右侧显示 `01/05` 格式步骤编号，使用 `Martian Mono 12px / 18px`。
- 背景为 `rgba(247,242,233,0.96)`。
- 下边框为 `1px solid #BCC7D7`。
- 使用 `position: sticky; top: 0; z-index: 30`。

### 8.2 Broadcast 标题栏

- 高度为所属面板高度的 `18.75%`。
- 背景使用标题栏固定渐变。
- 左侧图标为 `36×36px`。
- 标题与图标间距为 `12px`。
- 右侧时间使用 `Martian Mono 12px / 18px`。
- 标题使用 `#FFFF6F`；时间使用 `#FFFFFF`。

### 8.3 字幕条

- 高度为所属面板高度的 `16.6667%`。
- 背景为 `#354E84`。
- 顶部边框为 `1px solid rgba(255,255,255,0.78)`。
- 水平内边距为面板宽度的 `7.8125%`。
- 文字使用 `Martian Mono 12px / 18px` 或 `Noto Sans SC 13px / 20px`。
- 单次只显示 `1` 条结论，最多 `36` 个汉字。
- 不使用持续横向滚动文字。

### 8.4 按钮

- 主按钮高度为 `52px`，宽度为 `100%`。
- 次按钮高度为 `48px`。
- 图标按钮点击区域不得小于 `44×44px`。
- 按下态位移为 `translate(2px, 2px)`，阴影改为 `1px 1px 0 #20243B`。
- 禁用态透明度为 `0.4`，不执行位移动效。
- 焦点环为 `2px solid #332370`，offset 为 `2px`。

### 8.5 输入框

- 单行输入框高度为 `52px`。
- 多行输入框最小高度为 `104px`。
- 水平内边距为 `14px`。
- 标签与输入框间距为 `8px`。
- 聚焦边框为 `2px solid #332370`。
- 错误文字使用 `13px / 20px` 与 `#A63D4B`。

### 8.6 标签

- 普通标签高度为 `30px`，水平内边距为 `10px`。
- 可点击时段标签高度为 `40px`，水平内边距为 `12px`。
- 标签间距为 `8px`。
- 标签允许换行，不允许横向滚动。
- 选中态同时显示 `check` 图标和 `2px` 边框。

## 9. 产品组件

### 9.1 四类目标卡

- 四张卡按单列排列，间距为 `12px`。
- 折叠高度为 `68px`。
- 同时最多展开 `1` 张卡。
- 展开内边距为 `16px`。
- 工作卡只显示工作日、开始时间和结束时间。
- 其他三类固定显示目标、每周投入时间和截止日期。
- 孕产卡增加 `checkup-calendar` 与“添加固定产检”入口。

### 9.2 产检固定节点

- 条目最小高度为 `72px`。
- 左侧描边为 `3px solid #FFFF6F`。
- 条目背景为 `#332370`，文字为 `#FFFFFF`。
- 日期使用 `Martian Mono 14px / 22px`。
- 固定标签显示锁图标和“固定日期”。
- 用户可编辑或删除；AI 不显示移动操作。
- 冲突时只移动普通任务。

### 9.3 目标全景图

- 结果区使用 `BroadcastPanel`。
- 面板内显示未来 `4` 周摘要；面板外使用纵向 Timeline 展开全部节点。
- Timeline 轨道宽度为 `24px`，节点直径为 `12px`，连线宽度为 `2px`。
- 当前节点使用 `#E28144`，固定产检节点使用 `#FFFF6F`。
- 每个节点显示类别、名称、日期、预计用时和完成条件。
- 工作只显示固定时间区块，不生成里程碑。

### 9.4 本周容量板

- 顶部按顺序显示工作、固定产检、个人安排、剩余时间。
- 每组使用 `1` 行，行高不小于 `44px`。
- 总容量使用 `Martian Mono 20px / 28px`。
- 容量条高度为 `10px`，分为工作、固定事项、可用时间三段。
- 超出容量时显示“本周超出 X 分钟”，不使用警报图标。

### 9.5 本周任务卡

- 每张卡显示来源、目标、预计用时、完成条件、第一步、日期和时段。
- 卡片顶部使用 `64px` 标题栏，不使用图片。
- 完成条件区背景为 `#ECE8DE`，内边距为 `12px`。
- 第一步区域使用 `3px solid #E28144` 左侧描边。
- 同屏最多展开 `1` 张任务卡。

### 9.6 能量仪表

- 使用 `3` 个并列按钮，每个最小尺寸为 `88×72px`。
- 三档分别显示 `1/3、2/3、3/3` 和文字。
- 选中态边框为 `2px solid #FFFF6F`，背景为 `#354E84`，文字为 `#FFFFFF`。
- 状态卡顶部显示“状态确认 1/2”或“2/2”。
- 胎心线图案宽度为 `96px`，不显示 BPM。
- 第 `2` 次确认完成后保留编辑入口，不生成第 `3` 次询问。

### 9.7 今日行动卡

- 卡片只显示 `1` 个当前行动。
- 固定显示来源、预计时间、完成条件、时段和推荐原因。
- 主操作为“现在开始”。
- 次操作为“晚点可以”和“今天不行”。
- 点击“晚点可以”后，在原卡片字幕条显示下一时段和缩小后的时长。
- 当前行动使用 `#FFFF6F` 标题与 `#354E84` 内容区。

### 9.8 重排对比

- 使用上下排列。
- “调整前”透明度为 `0.6`。
- 中间箭头尺寸为 `24px`。
- “调整后”使用 `2px solid #E28144`。
- 只显示变化字段：任务长度、日期、时段或完成条件。
- 重排原因放入高度 `44px` 的字幕条。

### 9.9 总结板

- 使用 `BroadcastPanel` 显示完成数、投入分钟和完成比例三项数据。
- 面板外固定显示今日进展、系统发现、明日第一步三个模块。
- 数据图只允许横向条形、`7` 个圆点或 `7` 格日历。
- 单个数据图最多使用 `3` 个色值。
- 明日第一步使用 `baby-footprints` 或 `task-signal`，图案最大 `64×64px`。

## 10. 页面状态

### 10.1 空状态

- 图案尺寸为 `80×80px` 或 `96×96px`。
- 标题使用 `18px / 26px`。
- 说明最多 `3` 行。
- 最多显示 `1` 个主按钮和 `1` 个文字按钮。
- 页面 1 使用 `belly-profile`；页面 2 使用 `week-ring`；页面 3 使用 `fetal-heartline`；页面 4 使用 `task-signal`；页面 5 使用 `baby-footprints`。

### 10.2 加载状态

- 加载步骤固定为 `3–5` 项文字。
- 当前步骤使用 `#332370` 背景和 `#FFFF6F` 文字。
- 已完成步骤显示 `check` 图标。
- 骨架屏循环时长为 `1200ms`。
- 保留用户已填写内容。

### 10.3 成功状态

- 顶部显示结果标题和 `1` 句摘要。
- 结果面板使用 `BroadcastPanel` 或普通卡片，二者只选 `1` 种作为首个结果区。
- 保存提示显示 `2000ms` 后消失。

### 10.4 失败状态

- 失败提示放在原组件上方，不替换整页。
- 提示背景为 `#F8E0E3`。
- 提示包含错误原因、重试和手动处理入口。
- 用户输入、完成记录和调整记录保持不变。

## 11. 动效

| 动效 | 时长 | 缓动 | 数值 |
|---|---:|---|---|
| App 首次开机 | `480ms` | `ease` | `scaleX(0) scaleY(0.02)` → `scaleX(1) scaleY(0.02)` → `scale(1)` |
| 页面进入 | `220ms` | `cubic-bezier(0.2,0.8,0.2,1)` | `translateX(12px)` → `0`，`opacity 0` → `1` |
| 页面返回 | `220ms` | `cubic-bezier(0.2,0.8,0.2,1)` | `translateX(-12px)` → `0`，`opacity 0` → `1` |
| 卡片展开 | `180ms` | `ease-out` | 高度与透明度 |
| 颜色、边框 | `150ms` | `cubic-bezier(0.4,0,0.2,1)` | 起止色值 |
| 信息条进入 | `300ms` | `ease-out` | `translateY(8px)` → `0` |
| 信息条退出 | `50ms` | `ease-out` | `opacity 1` → `0` |
| 状态重排 | `240ms` | `ease-out` | 旧卡 `opacity 1` → `0.6`；新卡 `opacity 0` → `1` |
| 骨架屏 | `1200ms` | `ease-in-out` | 背景位置 `0%` → `100%` |

- App 开机动画每次会话只播放 `1` 次。
- 开机动画最大亮度为 `1.2`，不得使用 `brightness(10)`。
- 卡片列表错峰为 `40ms`，最多错峰 `5` 项。
- 扫描线为静态叠层，不执行循环位移。
- `prefers-reduced-motion: reduce` 时，循环动效停止，位移改为 `1ms` 透明度切换。

## 12. 五页映射

| 页面 | BroadcastPanel 内容 | 图案 | 底部主按钮 |
|---|---|---|---|
| 1. 目标设定与全景图 | `GOAL OVERVIEW`、四周节点数、固定产检数 | `belly-profile`、`checkup-calendar`、`hospital-bag` | 生成目标全景图 |
| 2. 本周时间与重点确认 | `WEEKLY CAPACITY`、可用小时、重点数 | `week-ring`、`task-signal` | 确认本周重点 |
| 3. 今日状态与任务 | `STATUS CHECK 1/2`、能量、可用分钟 | `fetal-heartline`、`pillow-moon`、`water-drop` | 生成今日行动 |
| 4. 行动执行与重排 | `CURRENT ACTION`、任务长度、下一时段 | `task-signal`、`stretch-figure` | 现在开始 |
| 5. 今日总结与明日计划 | `DAILY SIGN-OFF`、完成数、投入分钟 | `baby-footprints`、`weight-path` | 确认明日第一步 |

## 13. 无障碍

- 正文与背景对比度不得低于 `4.5:1`。
- `24px` 且字重不低于 `600` 的文字对比度不得低于 `3:1`。
- 点击区域不得小于 `44×44px`。
- 焦点环不得被 `overflow: hidden` 裁切。
- 类别、能量和状态不得只依赖颜色。
- 所有图案设置文字标签或 `aria-hidden="true"`；二者只选 `1` 种。
- 错误提示使用 `role="alert"`。
- 状态更新使用 `aria-live="polite"`。

## 14. 硬约束（Don't）

1. 不得使用 The Weather Channel 名称、标志、节目标题或原站图标文件。
2. 不得把整个 App 固定为 `4:3`；只有 `BroadcastPanel` 使用 `4:3`。
3. 不得在单页使用超过 `1` 个 `BroadcastPanel`。
4. 不得在单屏使用超过 `5` 个非中性色。
5. 不得使用 `#000000` 作为正文色；正文使用 `#20243B`。
6. 不得使用 `#A63D4B` 表示延期、休息、低能量或未完成。
7. 不得使用持续闪烁、亮度高于 `1.2` 或周期短于 `1000ms` 的循环动画。
8. 不得使用持续横向滚动字幕。
9. 不得在表单、正文或错误信息后方铺设图案。
10. 不得把胎心线图案解释为 BPM、胎动、风险或监测结果。
11. 不得使用超声图、胎儿照片或医疗诊断图作为装饰。
12. 不得让孕产类别只使用粉色；必须同时使用名称和图标。
13. 不得使用桌面顶部导航、侧边栏、网站 Hero、页脚或底部 Tab。
14. 不得生成需要横向滚动的甘特图、表格或周历。
15. 不得在同一首屏放置超过 `1` 个主按钮。
16. 不得让按钮或点击区域小于 `44×44px`。
17. 不得让固定底部操作区遮挡最后一项内容。
18. 不得自动移动用户标记为固定的产检日期。
19. 不得生成第 `3` 次每日能量询问。
20. 不得出现排行榜、连续打卡火焰、逾期红点或倒计时警报。

## 15. 验收清单

- `320×700px`、`390×844px`、`430×932px` 下无横向滚动。
- `768px` 和 `1440px` 宽度下，App 外壳不超过 `480px`。
- 五页均显示 `NN/05` 步骤编号。
- 五页均具备空、加载、成功和失败状态。
- 每页最多 `1` 个 `BroadcastPanel`。
- 每个 `BroadcastPanel` 的比例为 `4 / 3`，误差不超过 `0.5%`。
- BroadcastPanel 内部水平安全边距为宽度的 `7.8125%`，误差不超过 `1px`。
- 四类目标同时显示名称、图标和颜色。
- 工作类别不显示 AI 拆解。
- 其他三类显示目标、每周投入时间和截止日期。
- 固定产检节点没有移动操作。
- 页面 3 显示状态确认 `1/2`；页面 4 最多显示 `2/2`。
- 孕产图案使用本文件中的图案 ID、尺寸、描边和透明度。
- 所有正文对比度不低于 `4.5:1`。
- 所有点击区域不小于 `44×44px`。
- 开启减少动态效果后，不执行循环动画和位移动画。
- 页面使用的颜色、字号、间距、圆角和动效时长均来自本文件。

---

## 16. 响应式 Website 首页规范（已合并）

> 本节原始来源保存在 docs/reference/2026-08-10-today-homepage-website-design.md。本节是当前开发规则，不是可选参考。

### 16.1 目标

为已经完成目标设定和本周确认的孕期用户提供一个每天打开即可使用的网页首页。首页回答三个问题：

1. 我现在最适合做什么？
2. 如果状态变化，今天的任务会怎样调整？
3. 今天还有哪些固定事项和后续任务？

首页不承担目标创建、完整周计划编辑或日终分析生成。

### 16.2 成功条件

- 用户进入页面后，无需滚动即可看到当前能量、唯一当前行动和下一个产检节点。
- 用户在一次点击内可以修改能量档位。
- 修改能量后，当前行动和接下来两项任务在 `500ms` 内完成本地重排。
- 用户可以对当前行动执行“现在开始”“晚点可以”“今天不行”。
- “晚点可以”必须显示新的具体建议时段，不显示模糊的“稍后提醒”。
- 桌面宽度 `1280px` 下首屏不出现横向滚动；手机宽度 `390px` 下折叠为单列。

### 16.3 与全局规范的关系

色彩、字体、4px 间距系统、组件描边、孕产图案、动效时长、错误色和无障碍规则继续使用 `DESIGN.md`。

本页作为 Website 首页，覆盖以下两条 App 限制：

- 桌面内容外壳由最大 `480px` 改为最大 `1180px`。
- `768px` 以上允许双栏布局，不使用 App 底部固定操作栏。

其余硬约束继续生效，包括：单页最多一个 `BroadcastPanel`、不使用底部 Tab、不使用医疗监测含义、不自动移动固定产检日期、不出现打卡火焰或逾期警报。

### 16.4 响应式布局

#### 16.4.1 桌面

- 页面最小宽度：`1024px`。
- 内容外壳：`max-width: 1180px`，左右边距至少 `32px`。
- 顶部导航高度：`72px`。
- 主内容使用 `grid-template-columns: minmax(0, 1.45fr) minmax(320px, 0.8fr)`。
- 两栏间距：`24px`。
- 左栏包含状态总览、当前行动和接下来任务。
- 右栏包含状态修改、下一个产检和快捷入口。

#### 16.4.2 平板

- `768–1023px` 使用两栏：`minmax(0, 1fr) 300px`。
- 页面左右边距：`20px`。
- 当前行动内的操作按钮允许换行。

#### 16.4.3 手机

- 小于 `768px` 改为单列。
- 内容左右边距：`16px`。
- 排列顺序：状态总览 → 当前行动 → 状态修改 → 接下来任务 → 下一个产检 → 快捷入口。
- 不设置固定底部操作区。

### 16.5 页面结构

#### 16.5.1 顶部导航

- 左侧：孕肚轮廓图案、产品名“孕期目标推进助手”。
- 中间：今日、目标全景、本周计划、总结四个入口；“今日”处于选中状态。
- 右侧：孕周“孕 18 周”、日期“8 月 10 日 周一”和圆形用户入口。
- 手机仅显示产品名、孕周和用户入口；其他入口收进菜单按钮。

#### 16.5.2 今日状态总览

- 使用本页唯一的 `BroadcastPanel`。
- 面板比例：桌面 `16 / 6`，手机 `4 / 3`；这是对 `DESIGN.md` 结果面板比例的首页覆盖。
- 标题栏显示“今日状态”和“08:30 已确认 1/2”。
- 主区域左侧显示 `week-ring` 与“孕 18 周 + 3 天”。
- 主区域中间显示当前能量“2/3 一般”和可用时间“75 分钟”。
- 主区域右侧显示系统结论“今天保留 2 项；先做 25 分钟版本”。
- 字幕条显示“状态变化时可以再确认 1 次，系统只调整任务，不增加目标”。

#### 16.5.3 当前行动

- 卡片标题：“现在最适合做”。
- 只显示一项任务：核对待产包洗护用品清单。
- 同时显示来源类别、建议时段、预计用时、完成条件和推荐原因。
- 主按钮：“现在开始”。
- 次级操作：“晚点可以”“今天不行”。
- 点击“现在开始”后显示进行中计时条和“完成这一步”。
- 点击“晚点可以”后，任务移动到“晚上 19:30”，并把下一项提升为当前行动。
- 点击“今天不行”后，展示“缩小为 10 分钟 / 移到明天上午 / 从本周移除”三个明确选项。

#### 16.5.4 状态修改

- 标题：“现在的能量变了吗？”
- 三个档位：低能量、一般、有余力。
- 可用时间使用 `10 / 20 / 30 / 45 / 60+` 分钟按钮，不要求输入数字。
- 用户选择后显示预览：“将保留 1 项，共 10 分钟”或“将保留 3 项，共 75 分钟”。
- 点击“按当前状态调整”后更新当前行动和接下来任务。
- 每天第二次确认后，控件变为只读并显示“今日已确认 2/2”。

#### 16.5.5 接下来两项

- 最多显示两张任务卡。
- 每张卡显示类别、名称、建议时段、预计用时和当前状态。
- 不在这里显示完整完成条件。
- 任务完成、顺延或重排时使用 `300ms` 淡入和 `8px` 位移。

#### 16.5.6 下一个产检

- 使用 `checkup-calendar` 图案。
- 显示“8 月 15 日 09:00”“孕 19 周大排畸与唐氏筛查”“需空腹，携带产检本与身份证”。
- 显示“还有 5 天”，但不使用红色或倒计时动画。
- 只有“查看产检安排”入口，不提供自动改期。

#### 16.5.7 快捷入口

- 两个入口：“查看本周全景”“进入今日总结”。
- 入口使用文本按钮和右箭头，不使用大面积卡片。

### 16.6 视觉规则

- 页面外部背景：`#285D91`。
- 页面主体背景：`#F7F2E9`。
- 导航和卡片背景：`#FFFDF8`。
- 当前行动使用 `#E28144` 顶边和 `#FFFF6F` 当前标签。
- `BroadcastPanel` 使用 `#354E84` 主区、`#332370` 标题区和 `#E28144` 标题渐变。
- 中文字体：`Noto Sans SC, PingFang SC, Microsoft YaHei, system-ui, sans-serif`。
- 数字、日期和英文标签：`Martian Mono, ui-monospace, Consolas, monospace`。
- 卡片圆角：`16px`；按钮圆角：`10px`；标签圆角：`999px`。
- 卡片边框：`1px solid #BCC7D7`。
- 点击区域最小尺寸：`44×44px`。
- 图案只使用 `belly-profile`、`week-ring`、`task-signal`、`checkup-calendar`、`fetal-heartline`；图案不放在输入控件或正文后方。

### 16.7 数据与状态流

页面使用现有 `INITIAL_TODAY_TASKS`、`INITIAL_ENERGY_CHECKIN` 和 `INITIAL_CHECKUPS` 数据。

1. 页面加载时读取首次能量、今日任务和最近产检。
2. 按 `isCurrentAction` 识别唯一当前行动。
3. 用户修改能量和可用时间后，在前端根据任务长度、建议时段和顺序生成预览。
4. 用户确认后更新 `energyCheckin.checkinCount`、任务顺序和任务长度。
5. 用户执行操作后更新任务 `status` 和 `adjustmentNote`。
6. 本版不调用 Gemini API，不连接数据库；刷新页面后恢复演示数据。

### 16.8 页面状态

#### 16.8.1 空状态

- 显示“今天还没有推荐”。
- 保留状态修改模块。
- 主操作为“确认状态并生成今日推荐”。

#### 16.8.2 加载状态

- `BroadcastPanel` 保留尺寸，内容替换为三条骨架线。
- 当前行动卡保留高度 `280px`，显示骨架块。
- 加载文案：“正在匹配此刻可做的事”。

#### 16.8.3 成功状态

- 显示状态总览、唯一当前行动、接下来两项和下一个产检。
- 状态修改后显示重排原因。

#### 16.8.4 失败状态

- 已有任务和首次能量记录继续显示。
- 错误提示使用 `#F8E0E3` 背景和 `#A63D4B` 文字。
- 操作为“重新调整”和“保留当前安排”。

### 16.9 组件边界

- `HomeHeader`：只负责网页导航和孕周信息。
- `TodayBroadcastPanel`：只负责展示今日状态结果。
- `CurrentActionCard`：只负责唯一行动及三个处理动作。
- `EnergyAdjuster`：只负责第二次状态输入和重排请求。
- `UpcomingTasks`：只负责最多两项后续任务。
- `NextCheckupCard`：只读展示最近产检。
- `HomeQuickLinks`：负责进入其他五步页面。
- `TodayHomePage`：组合组件并持有演示状态。

每个组件通过明确的 props 接收数据和回调，不直接修改全局变量。

### 16.10 错误处理

- 本地重排失败时保留原任务顺序，不清空用户选择。
- 当前任务不存在时提升第一项未完成任务；没有未完成任务时进入空状态。
- 产检数据为空时显示“暂未添加产检”，并提供查看安排入口。
- 可用时间小于所有任务长度时，优先生成 `10` 分钟缩小版本。

### 16.11 验证范围

- TypeScript 类型检查通过。
- Vite 生产构建通过。
- `1440×900`、`1180×820`、`768×1024`、`390×844` 无横向滚动。
- 当前行动始终最多一项。
- 三个能量档位和五个时间按钮可点击。
- 三种当前行动处理操作均产生可见反馈。
- 第二次状态确认后不可再次提交。
- 最近产检不会因任务重排改变日期。
- 键盘可以访问所有按钮，焦点轮廓可见。
- `prefers-reduced-motion: reduce` 下禁用位移动画。

### 16.12 本版不做

- 不连接真实 AI、数据库、日历或消息推送。
- 不实现完整账户系统。
- 不重新设计五步页面。
- 不生成医疗建议或孕期风险判断。
- 不提供第三次每日状态确认。
- 不提供任务拖拽排序。
